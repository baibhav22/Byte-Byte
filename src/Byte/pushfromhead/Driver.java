package Byte.pushfromhead;

public class Driver {

    public static void main(String[] args) {
        PushFromHead pushData=new PushFromHead();

        pushData.pushfromhead(20);
        pushData.pushfromhead(30);
        pushData.pushfromhead(40);
        pushData.pushfromhead(50);
        pushData.pushfromhead(60);
        pushData.pushfromhead(70);


        //pushData.show();
    }
}

